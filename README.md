# SpringBootRestDataMysqlDockerComposeDemo

Spring-Boot + RepositoryRestResource + MySQL + Docker-Compose => Rest API in 5 Minutes :*)

# Quickstart

mvn clean install
(cd src/main/docker/ && docker-compose up)
(cd src/main/docker/ && docker-compose up)
